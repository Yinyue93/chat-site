<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - Glitch Chat</title>
    <link rel="stylesheet" href="/style.css"> <!-- Corrected link to your CSS -->
</head>
<body>

    <div class="form-container">
        <h1>Admin Login</h1>

        <% if (typeof error !== 'undefined' && error) { %>
            <p class="error"><%= error %></p>
        <% } %>

        <form action="/admin-login" method="POST">
            <div class="form-group">
                <label for="username">Admin Username:</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div class="form-group">
                <label for="password">Admin Password:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit" class="btn admin-btn">Login as Admin</button>
        </form>
        <p class="back-link"><a href="/">Back to User Login</a></p>
    </div>

</body>
</html>